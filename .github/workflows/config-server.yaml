name: CICD workflow for [config-server] service

on:
  workflow_dispatch: {}
  push:
    # paths:
    #   - ./.github/services/config-server.yaml
    branches:
  #     # - main
  #     # - develop
      - cicd


jobs:
  main-workflow:
    name: Call the main workflow
    uses: ./.github/workflows/cicd-test.yaml
    secrets: inherit