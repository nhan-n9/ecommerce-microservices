eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  cloud:
    config:
      override-system-properties: false     # do not override config from microservice
  datasource:
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:QHyCmtc8qthgigNO}

application:
  config:
    jwt:
      secret: ${JWT_SECRET:a00ls1hLRMxTgk0AcrpawYJvR4FDwc07xGyaROb0H6L}