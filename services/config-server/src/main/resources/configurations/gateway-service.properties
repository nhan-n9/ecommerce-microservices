server.port=8080

# Enable service discovery for the gateway -> gateway can find & route requests to registered services
spring.cloud.gateway.server.webflux.discovery.locator.enabled=true
# Convert service IDs to lower case -> can use lower-case service IDs from urls
spring.cloud.gateway.server.webflux.discovery.locator.lower-case-service-id=true

# Strip /{serviceId} prefix for discovery-locator routes so identity-service receives /api/v1/identity/**
spring.cloud.gateway.server.webflux.discovery.locator.filters[0]=StripPrefix=1

# Apply JWT auth filter to all routes (including discovery-locator routes)
spring.cloud.gateway.server.webflux.default-filters[0]=AuthFilter